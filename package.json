{
  "name": "livepeer-tools-ui",
  "private": true,
  "scripts": {
    "clean": "rm -Rf packages/*/dist/ packages/*/node_modules/ rm -Rf packages/tools-ui/public/ai-generator",
    "build:ai-generator-ui": "cd packages/ai-generator-ui && npm install && npm run build",
    "build:tools-ui": "cd packages/tools-ui && npm install && npm run build",
    "build": "npm run clean && npm run build:ai-generator-ui && npm run build:tools-ui"
  }
}
