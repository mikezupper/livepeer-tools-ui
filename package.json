{
  "name": "livepeer-tools-ui",
  "private": true,
  "scripts": {
    "clean": "rm -Rf packages/*/dist/ packages/*/node_modules/",
    "build:tools-ui": "cd packages/tools-ui && npm install && npm run build",
    "build": "npm run clean && npm run build:tools-ui"
  }
}
